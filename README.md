# Connection Speed Check

Connection Speed Check is a GitHub Pages web app that estimates browser download speed from a live test payload.
The repository also includes speed_test.py, a standard-library Python script for checking the current connection from the terminal.
Open index.html in a browser to view the web app, or run python3 speed_test.py --runs 3 for the local script.
